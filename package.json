{
  "name": "nethrox",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "npx serve frontend/dist -s -l tcp://0.0.0.0:${PORT:-3000}"
  }
}
